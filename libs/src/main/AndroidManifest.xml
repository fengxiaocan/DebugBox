<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.box.libs" >


    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <application>
        <activity android:name="com.box.libs.ui.Dispatcher"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/PdTheme"/>
        <activity android:name="com.box.libs.ui.TransActivity"
            android:theme="@style/PdTheme.Trans"/>

        <provider
            android:name="com.box.libs.DebugBox"
            android:authorities="${applicationId}.pdFileProvider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_path" />
        </provider>
    </application>


</manifest>
